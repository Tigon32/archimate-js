#!/usr/bin/env sh
set -eu

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

printf '%s\n' 'archimate-js: running deterministic local pre-push gate (npm run verify:local)'
npm run verify:local
